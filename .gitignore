node_modules/
.astro/
dist/
.env
.env.local
.DS_Store
*.log

# Generated species/period data : committed pour reproductibilité du build
# Cloudflare Pages. Re-générer via `npm run import:paleobiodb`.
# (Ligne d'ignore retirée volontairement — le JSON est versionné.)

# Heavy GIF sources (>25 MB Cloudflare cap). Convert to MP4/WebM and version those instead.
frontend/public/*.gif
frontend/public/**/*.gif

# Windows Zone.Identifier ADS leak from WSL/copy from Windows.
*:Zone.Identifier
*.Identifier
